Drop .png, .jpg, .jpeg, .bmp, or .gif files in this folder.

Each one is automatically converted to full-color ASCII art at startup and
added to the pool the ASCII Bomb (and "All") prank picks from at random.

After adding/removing images, rebuild (dotnet build or dotnet run) so they
get copied into the output folder.
